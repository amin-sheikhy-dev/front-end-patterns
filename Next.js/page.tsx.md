app/page.tsx

این فایل محتوای واقعی که کاربر می‌بینه رو مشخص می‌کنه.

هر `page.tsx` یک راوت `(Route)` ایجاد می‌کنه.

مسیر / به این صفحه می‌رسه.

```tsx
export default function HomePage() {
  return (
    <main>
      <h1>به سایت من خوش آمدید! 🚀</h1>
      <p>این صفحه اصلی هست.</p>
    </main>
  );
}
```
